# Leave blank during local development to use Vite's /api proxy.
# Set this to the deployed API origin for a separately hosted frontend.
VITE_API_URL=

# Render Blueprints set this automatically from the API service hostname.
VITE_API_HOST=
